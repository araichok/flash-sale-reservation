Flash Sale Reservation Service

Сервис резервирования товаров для сценариев flash-sale с поддержкой временных резервов, конкурентного доступа, Redis и Outbox-паттерна.

🛠 Стек технологий

Go (net/http, chi)

PostgreSQL

SQL migrations (ручные .sql миграции)

Redis

Outbox pattern

Docker Compose (PostgreSQL + Redis)

📌 Основная идея

Товар можно зарезервировать на фиксированное время (5 минут)

Во время резерва stock уменьшается

Резерв можно:

подтвердить → становится финальным

отменить → stock возвращается

истечь по времени → stock возвращается

Для одного (user_id, product_id) нельзя иметь больше одного ACTIVE резерва

Redis используется для:

TTL резерва

метрик

При подтверждении резерва используется Outbox pattern

🧱 Архитектура

products — управление товарами и остатками

reservations — бизнес-логика резервов

outbox_events — события для асинхронной обработки

Redis — TTL + метрики

PostgreSQL — источник истины

🗄 База данных

Используются SQL-миграции.

Основные таблицы:

products

reservations

outbox_events

Ограничение:

один ACTIVE резерв на (user_id, product_id) (через unique index)

🚀 Запуск проекта
1️⃣ Запуск PostgreSQL и Redis
docker-compose up -d

2️⃣ Применить миграции
psql reservation_db < migrations/000001_init.up.sql

3️⃣ Запуск сервиса
go run cmd/app/main.go


Сервис будет доступен на:

http://localhost:8080

📦 API
🔹 Создать товар

POST /products

{
"name": "iPhone 15",
"stock": 10
}

🔹 Получить список товаров

GET /products

🔹 Создать резерв

POST /reservations

{
"product_id": 1,
"user_id": 42
}


Результат:

резерв создаётся на 5 минут

stock уменьшается на 1

🔹 Получить резерв по ID

GET /reservations/{id}

🔹 Подтвердить резерв

POST /reservations/{id}/confirm

статус → CONFIRMED

stock не возвращается

в outbox_events записывается событие ReservationConfirmed

🔹 Отменить резерв

POST /reservations/{id}/cancel

статус → CANCELED

stock возвращается (+1)

🔹 Список резервов

GET /reservations

Параметры:

user_id

status

limit

offset

🛠 Admin API
🔹 Синхронизация истёкших резервов

POST /admin/reservations/sync-expired

Что делает:

находит резервы с expires_at < now

переводит их в EXPIRED

возвращает stock

увеличивает Redis-метрику

Ответ:

{
"expired": 3
}

⏱ Время жизни резерва

Резерв живёт 5 минут

expires_at хранится в PostgreSQL

Redis используется только как таймер (TTL)

Истина всегда в БД

📊 Redis

Используется для:

TTL
reservation:{id} → TTL = expires_at

Метрики
metrics:reservations:created
metrics:reservations:confirmed
metrics:reservations:canceled
metrics:reservations:expired

📤 Outbox Pattern

При подтверждении резерва:

обновляется статус резерва

в той же транзакции создаётся запись в outbox_events

Пример события:

{
"event_type": "ReservationConfirmed",
"payload": {
"reservation_id": 1,
"product_id": 2,
"user_id": 42,
"confirmed_at": "2026-02-14T12:30:00Z"
}
}


Это гарантирует, что событие не потеряется.

🔒 Конкурентная безопасность

Транзакции

SELECT … FOR UPDATE

stock > 0 при обновлении

защита от гонок при последней единице товара